workflows:
  ios-workflow:
    name: iOS Build & Archive
    max_build_duration: 60
    scripts:
      - name: Install npm dependencies
        script: |
          npm ci
      - name: Build web assets
        script: |
          npm run build
